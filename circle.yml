machine:
  node:
    version: 6.2.1

test:
  override:

compile:
  override: npm run build

deployment:
  production:
    branch: v4
    commands:
      - surge --project ./dist --domain podlove-player
