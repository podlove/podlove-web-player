machine:
  node:
    version: 6.2.1

compile:
  override:
    - npm run build

test:
  post:
    - bash <(curl -s https://codecov.io/bash)

deployment:
  production:
    branch: v4
    commands:
      - surge --project ./dist --domain podlove-player.surge.sh
